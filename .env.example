# Copy to .env for local runs (mvn spring-boot:run picks up exported shell
# vars, not .env directly - export these or use `set -a && source .env`).
# .env itself is gitignored - never commit real values here.

DEMO_PASSWORD=changeme
ANTHROPIC_API_KEY=
MAX_ANALYSES_PER_DAY=20
